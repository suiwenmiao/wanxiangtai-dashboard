---
name: wanxiangtai-dashboard
description: Download and validate Wanxiangtai item and creative reports, refresh the Vue dashboard, and operate the local or GitHub Pages deployment pipeline. Use for daily report automation, material-level creative analysis, category matching, dashboard data integrity, and troubleshooting report downloads.
---

# 万相台每日数据下载与看板流程

## 8. 数据计算核心原则：绝不估算

### 8.1 铁律
**所有展示在看板上的数据，必须是在筛选时间范围内对原始数据进行实际分类求和的结果，绝不允许使用比例推算、分摊、按权重缩放等任何形式的估算。**

估算 = 错误。不可接受。

### 8.2 正确的计算方式
数据流水线按所需维度（日期、品类、主体、计划、场景等）对原始行数据进行 `groupby + sum` 聚合，生成预汇总数据。前端仅做过滤过滤求和（filter + reduce），不做任何比例计算。

```
原始大表（63,911行，含日期+品类+主体+场景+计划）
  ↓
实际汇总（按筛选维度 groupby + sum）
  ↓
前端直接过滤求和
```

### 8.3 哪些地方用了实际汇总（正确 ✓）
| 数据源 | 分组维度 | 记录数 | 用途 |
|---|---|---|---|
| `records` | 日期 + 品类 | 1,082 | 第一页 KPI / 趋势图 / 品类表 |
| `categoryScenarioRecords` | 日期 + 品类 + 场景名字 | 3,098 | 渠道推广概览、饼图 |
| `subCategoryRecords` | 日期 + 品类 + 细类 | 5,498 | 细类下钻 |
| `subjectDateRecords` | 日期 + 主体ID | 23,647 | 第二页主体表 |
| `subjectPlanRecords` | 日期 + 主体ID + 计划ID | **63,911** | **展开计划详情** |

### 8.4 哪些情况容易误用估算（错误 ✗）
| 错误做法 | 说明 | 正确做法 |
|---|---|---|
| `ratio = subject.cost / full.cost; plan.cost * ratio` | 用全周期计划花费按比例推算筛选期的花费 | 预汇总 `subjectPlanRecords`，前端直接过滤 |
| `subject.orders × (scenario.cost / subject.cost)` | 用主体总订单按花费占比分摊到场景 | 场景订单直接在数据流水线中 `sum` |
| `scenario.clicks * (subject.cost / full.cost)` | 用全周期场景点击量按花费比例缩放 | 场景点击量在 `categoryScenarioRecords` 中实际汇总 |

### 8.5 如何判断是否正确
1. 检查数据流水线：是否在 `build_xxx` 函数中用 `groupby + sum` 预汇总
2. 检查前端：`getScenarioGroups`、`scenarioSummary` 等函数是否直接用 `payload.xxxRecords` 做 `filter + reduce`，不出现 `* ratio` 或 `/ full.cost` 等比例表达式
3. 验证方法：选「全部」日期范围时，汇总后数据必须等于原始数据直接求和（`差异=0`）

### 8.6 新增维度时的标准流程
当需要按新的维度（如计划、细类、场景等）展示数据时：

1. **数据流水线**：在 `generate_dashboard_data.py` 中新增 `build_xxx_records(df)` 函数，用 `pandas.groupby([维度列]) + agg(sum)` 做实际汇总
2. **JSON 字段**：将结果加入 payload
3. **前端**：组件中直接用 `payload.xxxRecords.filter(...).reduce(...)` 获取数据
4. **禁止**：在前端或数据流水线中用全周期数据乘以比值来推算筛选期数据

### 8.7 检查清单
- [ ] `getScenarioGroups` 无 `ratio = subject.cost / full.cost`
- [ ] `scenarioSummary` 直接使用 `categoryScenarioRecords`
- [ ] 所有 `build_*` 函数只做 `groupby + sum`，不做比例运算
- [ ] `displaySubjects` 的 `orders` 来自 `subjectDateRecords`（日期筛选后），不是 `...meta`（全周期）
## 1. 目的
确保每天下载的万相台报表格式与看板数据流水线一致，避免因数据格式不匹配导致的场景数据丢失。

## 2. 正确数据格式标准

### 2.1 CSV列数
不要把固定列数作为硬性标准。万相台导出字段会变化，历史上出现过 79 列，当前也出现过 85 列。

正确格式的判断标准是：必须包含商品主体关键字段。每日自动下载必须在「商品数据明细」里把「维度」设置为「全选」，让导出同时包含商品、计划、时间维度。

### 2.2 关键列
| 索引 | 列名 | 必须 |
|---|---|---|
| 0 | 日期 | ✓ |
| 1 | 主体ID | ✓ |
| 2 | 场景ID | 可选 |
| 3 | 场景名字 | 可选 |
| 4 | 原二级场景ID | 可选 |
| 5 | 原二级场景名字 | 可选 |
| 6 | 计划ID | 可选 |
| 7 | 计划名字 | 可选 |

缺少 `日期` 或 `主体ID` 为错误格式，数据不可用。按标准流程全选维度后，报表应包含计划/时间明细；如果平台临时缺少场景/计划列，流水线会补默认值避免中断。

### 2.3 场景名字的值
- 如果导出包含场景明细，通常会包含：`人群推广`、`关键词推广`、`货品全站推广`
- 如果导出只有商品汇总，场景会补为：`未分类`

## 3. 下载操作标准流程

### 步骤1：商品数据明细维度全选
进入「报表 -> 商品报表 -> 商品数据明细」，在点击「下载报表」前，打开「维度」选择器并选择「全选」。

原因：计划报表会缺少 `主体ID` / `主体名称`，不能进入当前大表和看板；商品报表至少应包含 `日期` 和 `主体ID`。

### 步骤2：点击「下载报表」
在弹窗中把日期范围明确设置为「昨天」，时间粒度保持「分天」。不允许使用「过去 7 天」或其他多日范围。

### 步骤3：点击「确定」
提交下载任务。

### 步骤4：下载完成
从下载任务管理页下载刚创建的精确任务文件，不能用宽泛前缀误下载旧任务。

## 4. 数据流水线标准流程

下载完成后必须依序执行：

1. `wanxiangtai_download.py download` -> 下载CSV、验证关键字段、只保留目标日期、匹配品类/细类并更新大表
2. `generate_dashboard_data.py` -> 生成前端JSON
3. `pnpm build` -> 构建静态站点
4. `git push` -> 触发GitHub Pages部署

`match_category.py` 仅作为手动排障/补救工具，不应在每日流水线里再次追加数据。

### 4.1 创意素材看板（本地验证流程）

创意素材看板与商品日报分开运行。未明确要求上线时，只更新本地 JSON 和本地开发服务器，不运行部署或 `git push`。

1. 进入「报表 -> 创意报表 -> 创意数据明细」，先将页面「维度」设为「全选」。
2. 点击「下载报表」，在弹窗中选择：
   - 日期范围：`昨日`
   - 数据指标：`全部数据指标`
   - 数据粒度：`素材粒度报表`
3. 点击弹窗「确定」，到下载管理页按精确任务名下载；任务名必须以 `创意报表_` 开头。
4. 运行 `automation/creative_report_probe.py`，验证 ZIP、CSV 日期和字段。
5. 运行 `automation/dedupe_creative_images.py`，按感知哈希合并视觉近似的图片素材。
6. 运行 `automation/generate_creative_data.py`，按 `主体ID` 匹配基础表品类，生成 `frontend/src/data/creative-data.json`。
7. 启动 `pnpm dev` 或 Vite 本地服务器检查素材看板；仅在用户要求后再构建、提交或部署。

素材粒度报表必须至少包含：`日期`、`主体ID`、`计划ID`、`计划名字`、`创意ID`、`素材ID`、`素材名称`、`素材内容`、`素材类型`。`素材内容`为 URL 且`素材类型=图片`时，才作为图片矩阵素材；无图片 URL 的记录通常是标题/文案，不应误标为商品主图。

### 4.2 素材矩阵和筛选规则

- 矩阵列：按视觉图片指纹去重后的图片；同图不同 URL、创意或计划只显示一个代表列。
- 矩阵行：计划；单元格聚合实际展现、点击和订单，并展示 CTR、点击量、CVR。
- 高点击：只在实际聚合后的素材-计划单元格上标识，不用估算值。
- 筛选维度：商品 ID、计划、推广场景均支持多选；点击「筛选」后必须同步作用于 KPI、矩阵和素材点击明细。
- 标题/文字素材保留在明细表中，但默认不进入图片矩阵。

### 4.3 创意报表验证

每次创意导出后检查：

- CSV 的最小和最大 `日期` 都等于目标日期。
- 导出任务名称和下载文件名称均为创意报表，不得误下载商品报表。
- 存在素材字段和至少一条图片 URL 后，才允许生成图片矩阵。
- 图片去重映射写入 `data/reports/创意图片视觉去重_<日期>.json`；图片下载失败时保留原 URL，不得丢弃对应投放数据。
- 用户要求“本地测试”时，不得修改 `site/` 后提交、推送或部署线上。

## 9. 品类日报生成与数据准确性

适用于看板“投放日报 / 推广日报”的手机、DT、显示器品类筛选。日报是动态页面：选择报告日期后，必须使用该日与自然日昨天分别聚合，不能复用同一天数据，也不能沿用示例数值或品类词。

### 9.1 权威数据源与指标口径

- 唯一权威源：`~/Workbuddy/万相台数据表.xlsx` 的配置工作表（默认 `3月`）。人群或创意报表不能替代商品主表计算日报总览。
- 今日：`日期 == reportDate`；昨日：`日期 == calendarDay(reportDate - 1)`。日期计算必须按日历日（UTC-safe），不得受浏览器时区影响。
- GMV = `总成交金额`；直接 ROI = `直接成交金额 / 花费`；总 ROI = `总成交金额 / 花费`；CPA = `花费 / 总成交笔数`；CPC = `花费 / 点击量`；CTR = `点击量 / 展现量`；CVR = `总成交笔数 / 点击量`。
- 收藏指标必须使用 `总收藏数`，不得误用 `收藏宝贝数`。购物车使用 `总购物车数`，收藏加购使用 `总收藏加购数`。
- 平台撬动三项为 `自然流量转化金额`、`平台助推总成交`、`补贴引导成交金额`。它们是 **GMV 内部组成**，只可显示金额、环比和 GMV 占比，绝不再额外加到 GMV。

### 9.2 生成前强制校验

每次生成日报或刷新日报数据前，按“日期 + 品类”直接从源表 `groupby + sum` 计算今日、昨日，并与 `dashboard-data.json` 的同日期同品类记录逐项比对：

`花费、总成交金额、直接成交金额、总成交笔数、点击量、展现量、总购物车数、总收藏数、总收藏加购数、自然流量转化金额、平台助推总成交、补贴引导成交金额`。

- 两边每个字段一致才可构建、加密或发布。
- 若有差异，停止发布，优先检查日期是否重复/错位、字段映射是否错误、是否误用了辅助报表。
- 报告日期与对比日期必须同时显示在页面头部；不得把昨日数据写成今日，或把同一日期同时用于两侧。

### 9.3 日报表格与计划口径

- 固定模块顺序：核心结论、KPI 卡片、总体指标日环比、场景维度、细类维度、计划维度、TOP 商品主体、平台撬动贡献。
- 场景、细类、计划、主体均从原始明细实际聚合；禁止按占比、权重或全周期比例估算筛选期数据。
- 计划维度键为 `计划ID + 计划名字`，今日和昨日做并集以保证新增、减投和暂停记录都能在表中对比。
- 日报结论中的“今日在投计划数”沿用日报计划维度的并集展示口径，以保证与表内计划行数一致；手机当前示例为 15。
- 动作标签：昨日无投放 = `新增`；花费环比 >= 15% = `加投`；<= -15% = `减投`；其他 = `持平/微动`。
- 禁止在结论中使用“计划消失”“数据缺失”等机械表达。用“新增、加投、减投、持平/微动、结构调整明显/平稳”表达。
- 商品主体页：仅在品类筛选为 `DT` 时展示当前排序 TOP 10；手机、显示器及其他单品类筛选展示全部匹配主体。

### 9.4 六条核心结论模板

结论必须由真实日报数据即时填充，保持以下句式、顺序和语气；不硬编码 SKU、计划名、细类名或百分比。

1. `整体：GMV升/降、ROI升/降（双升，效率提升 / 量增价跌 / 量减效升 / 双降，效率承压）。总花费…、GMV…、总ROI 昨日→今日…、成交成本…。`
2. `场景结构：场景A（加投/减投/持平/微动，花费…，GMV…，ROI 昨日→今日）；场景B…；其中主力场景为今日 GMV 主力来源（GMV…，占品类…）。`
3. `计划加减法：今日在投 X 个计划。新增 X 个（最多列两个计划名）；加投 X / 减投 X / 持平/微动 X，结构调整明显/平稳。`
4. `细类格局：GMV 第一细类是绝对主力（GMV…，占品类…）；ROI 第一细类 ROI 最高（…），性价比突出；若存在花费大于 0 且 GMV 近 0 的细类，追加“属于长尾/测试”。`
5. `平台撬动：自然流量+平台助推+补贴合计贡献今日 GMV 的…（合计…）。自然流量转化…、平台助推成交…、补贴引导成交…——三项均为 GMV 内部组成。`
6. `蓄水指标：总收藏…、总购物车…、加购率…、收藏加购合计…——蓄水侧整体向好/仍有承压。`

### 9.5 日报交付验证

1. 运行 `automation/generate_dashboard_data.py`，再运行 `automation/encrypt_creative_data.mjs` 和 Vue build；不要手改加密 JSON。
2. 本地打开 `#/daily`，依次切换手机、DT、显示器；确认品类、报告日期、昨日日期、KPI 和表格同步变化。
3. 检查当日与昨日的源表汇总、私有 JSON、日报页面数值一致；重点复核收藏数和平台三项。
4. 用户未明确要求上线时，只构建本地 `site/`，不提交、不推送。

### 9.6 主体与计划的优化建议口径

- 页面可以展示单日或用户筛选区间的数据，但“加投、减投、暂停”不得依据单日表现判断。
- 动作建议统一取报告结束日向前的近 7 个自然日，并与此前 7 个自然日比较；主体对比品类均值，计划对比同品类、同推广场景均值。
- 只有近 7 日同时满足至少投放 4 天、花费不少于 ¥500、点击不少于 100，才允许给出预算动作；未满足时标记“观察样本”。
- “建议加投”要求 ROI 至少优于对照均值 15%，CPC 和加购率不弱于均值，并且相较前 7 日没有明显走弱；加投只建议分步上调 10%-20%。
- “建议减投”要求样本充足且 ROI 连续两个周期偏弱或较前 7 日明显回落；“建议暂停/换素材”还必须同时满足有足量点击、无成交、加购率显著低于对照均值。其余情况只给出 CPC、转化链路等优化方向或维持观察。

## 5. 格式验证

每次下载后自动在 `wanxiangtai_download.py` 中执行：
- 检查CSV是否包含 `日期`、`主体ID`
- 如果缺少 `场景名字`、`计划ID`、`计划名字` 等可选字段，补默认值继续处理
- 下载前必须把商品数据明细的「维度」设为「全选」
- 下载弹窗必须选择「昨天」；如果仍是「过去 7 天」则停止
- 下载弹窗时间粒度必须是「分天」
- 如果平台异常返回多日数据，只保留目标日期写入大表作为兜底保护
- 不通过则记录错误日志并停止部署

## 6. 常见问题

Q: 格式又错了怎么办？
A: 先看是否缺少 `日期` 或 `主体ID`。如果缺少，删除该CSV文件并重新下载商品报表；如果只是缺少场景/计划列，流水线会自动补默认值。

Q: 大表已有该日期的数据，想重新下载替换？
A: 先删除大表中该日期的数据行，再重新执行完整流水线。

## 7. 关键脚本位置

| 脚本 | 路径 |
|---|---|
| 下载脚本 | `automation/wanxiangtai_download.py` |
| 创意下载与校验 | `automation/creative_report_probe.py` |
| 创意图片视觉去重 | `automation/dedupe_creative_images.py` |
| 创意看板数据生成 | `automation/generate_creative_data.py` |
| 匹配脚本 | `automation/match_category.py` |
| 数据生成 | `automation/generate_dashboard_data.py` |
| 完整流水线 | `automation/run_daily.py` |
| 商品ID基础表 | `~/Desktop/商品ID基础表最新6.23.xlsx` |
| 大表 | `~/Workbuddy/万相台数据表.xlsx` |
